# thefoxup - ShellCheck configuration
# https://github.com/Morphilab/thefoxup

# Severity level (error, warning, info)
severity=warning

# Exclude source-following noise (sources resolve when all scripts are passed together)
exclude=SC1091

# Target shell
shell=bash
